import 'package:flutter/material.dart';

class ExplicitIcon extends StatelessWidget {
  const ExplicitIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.explicit_rounded);
  }
}
